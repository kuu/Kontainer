{
  "name": "kontainer-js",
  "version": "1.5.0",
  "description": "A media file format generator/parser that exposes a React-like API.",
  "repository": {
    "type": "git",
    "url": "https://github.com/kuu/Kontainer.git"
  },
  "main": "src/index.js",
  "bin": {
    "kontainer": "src/cli.js"
  },
  "directories": {
    "src": "./src",
    "test": "./test"
  },
  "scripts": {
    "lint": "eslint src/ test/",
    "test": "npm run lint && npm run test-node && npm run test-browser",
    "test-node": "jasmine JASMINE_CONFIG_PATH=jasmine.json",
    "test-browser": "karma start karma.conf.js",
    "clean": "rm -fR concatenated ; mkdir concatenated ; rm -fR minified ; mkdir minified",
    "build": "npm run clean && npm run bundle",
    "bundle": "browserify -t babelify src/index.js > concatenated/kontainer.js --debug",
    "release-build": "npm run clean && npm run release-bundle && npm run minify",
    "release-bundle": "NODE_ENV=production browserify -t babelify src/index.js > concatenated/kontainer.js",
    "minify": "uglifyjs concatenated/kontainer.js --source-map-url kontainer.js.map -p 1 --source-map minified/kontainer.js.map -c -m -o minified/kontainer.js",
    "prepublish": "dist-es6"
  },
  "author": "Kuu Miyazaki",
  "license": "MIT",
  "keywords": [
    "multimedia",
    "file format",
    "container format",
    "video",
    "audio",
    "MP4",
    "ISO BMFF",
    "ISO/IEC 14496-12"
  ],
  "devDependencies": {
    "babel-eslint": "^6.0.2",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "eslint": "^2.6.0",
    "jasmine-core": "^2.4.1",
    "jasmine-es6": "^0.1.8",
    "karma": "^0.13.22",
    "karma-browserify": "^5.0.3",
    "karma-chrome-launcher": "^0.2.3",
    "karma-cli": "^0.1.2",
    "karma-jasmine": "^0.3.8",
    "uglify-js": "^2.6.2",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "dist-es6": "^0.1.13",
    "yargs": "^4.2.0"
  }
}
